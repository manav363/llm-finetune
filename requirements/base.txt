# Backend-agnostic deps — enough to run data prep, split, config, the
# mock-backend dry-run, and the FastAPI serving layer offline (mock engine)
# with no GPU and no model downloads.
pyyaml>=6.0
datasets>=2.16

# Serving (M5): FastAPI inference endpoint. The real model engines are optional
# backend extras (cuda / mac); the mock engine runs with just these.
fastapi>=0.110
uvicorn>=0.29
httpx>=0.27

# Dev / quality
pytest>=8.0
ruff>=0.5
mypy>=1.10
types-PyYAML>=6.0
